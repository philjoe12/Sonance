[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "sonance"
version = "0.1.0"
description = "Prototype stereo-to-surround engine"
requires-python = ">=3.10"
